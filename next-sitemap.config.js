/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: "https://www.japandevnextjsapp.shop", // Your live production URL
    generateRobotsTxt: true, // Enable robots.txt file
  };
  
  module.exports = config;
  
  